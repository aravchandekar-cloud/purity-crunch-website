---
phase: 03-contact-form-polish
plan: 01
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [index.html, css/styles.css]
autonomous: false
---

<objective>
## Goal
Improve the export inquiry form UX for higher conversion, add trust signals, expand country options, add phone field, and apply final cross-site polish to complete the v0.1 milestone.

## Purpose
The form is the single conversion point — every improvement directly serves the core value: "B2B buyers trust the site enough to initiate a partnership inquiry within one visit." A more complete form with trust signals reduces friction and increases submissions.

## Output
- Enhanced form with phone field and expanded countries
- Trust signals near the form (response time, no commitment)
- Final responsive and visual polish across all sections
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/01-visual-identity-refresh/01-01-SUMMARY.md
@.paul/phases/02-testimonials-enhancement/02-01-SUMMARY.md
Phase 1: new color/font identity. Phase 2: testimonials + process images.

## Source Files
@index.html (form at lines 603-670, export-section on dark background)
@css/styles.css (form styles at lines ~1065-1103)
@js/main.js (form submission handler — DO NOT MODIFY, only verify webhook works)
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /frontend-design | required | Before form redesign | ○ |
| /ui-ux-pro-max | required | For form UX patterns and conversion optimization | ○ |

**BLOCKING:** Both skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /frontend-design loaded
- [ ] /ui-ux-pro-max loaded

</skills>

<acceptance_criteria>

## AC-1: Enhanced Form Fields
```gherkin
Given the export inquiry form is visible
When inspecting the form fields
Then a phone number field with type="tel" exists between email and country, and the country dropdown includes at least 15 countries covering major B2B import markets
```

## AC-2: Trust Signals Visible
```gherkin
Given a B2B buyer views the export inquiry section
When looking at the area around the form
Then they see at least 2 trust signals (e.g., "Response within 24 hours", "No commitment required", "Free samples available") that reduce submission friction
```

## AC-3: Form Webhook Intact
```gherkin
Given the form's Google Sheets webhook configuration
When inspecting the form action and js/main.js submission handler
Then the webhook URL and submission logic are unchanged from the original — no JS modifications made
```

## AC-4: Final Responsive Polish
```gherkin
Given the website is viewed at 375px, 768px, 1024px, and 1440px viewports
When scrolling through all sections (hero, trust, products, videos, heritage, process, testimonials, FAQ, export, footer)
Then no layout breaks, overflow, or visual inconsistencies exist at any breakpoint
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Enhance form fields and add trust signals</name>
  <files>index.html</files>
  <action>
    Modifications to the export inquiry section in index.html:

    1. Add phone number field:
       - Insert after the email form-row, before the country row
       - Create a new form-row with phone field on left:
         - label: "Phone Number" (no required star — optional field)
         - input type="tel", id="phone", name="phone", placeholder="+91 98765 43210" (use India-based placeholder since the company is Indian), autocomplete="tel" <!-- audit-added: changed placeholder from US-centric "+1 (555) 000-0000" to India-based format — this is an Indian export company serving global B2B buyers; a US placeholder implies US origin -->
         - Add pattern attribute: pattern="[\d\s\+\-\(\)]{7,20}" and title="Enter a valid phone number with country code" for basic client-side format guidance <!-- audit-added: tel input has no built-in format validation; pattern provides guardrails without blocking international formats -->
       - Move country select to right side of the same row as phone

    2. Expand country dropdown:
       - Replace the 7-option list with a comprehensive B2B import market list (~20+ countries):
         - United States, United Kingdom, Canada, Australia, Germany, France, Netherlands, Belgium, Singapore, Malaysia, Japan, South Korea, UAE, Saudi Arabia, Qatar, Kuwait, South Africa, Nigeria, Kenya, Brazil, Mexico, New Zealand, Ireland, Switzerland, Other

    3. Add trust signals below the "micro-copy" paragraph, before the form-container:
       - Create a trust-signals div with 3 items, using a <ul> with role="list" for semantic correctness: <!-- audit-added: trust signals are a list of value propositions; <ul> is semantically correct and aids screen readers -->
         - <li> Icon (ph-clock, aria-hidden="true") + "Response within 24 hours" </li> <!-- audit-added: aria-hidden on decorative icons -->
         - <li> Icon (ph-handshake, aria-hidden="true") + "No commitment required" </li>
         - <li> Icon (ph-package, aria-hidden="true") + "Free samples for qualified buyers" </li>
       - Style as a horizontal flex row with subtle styling (list-style: none, remove default padding)

    4. Fix autocomplete attribute on existing country select: <!-- audit-added: autocomplete="country" is non-standard; the correct token is "country-name" per WHATWG spec -->
       - Change autocomplete="country" to autocomplete="country-name" on the country <select>

    5. Webhook compatibility warning — add HTML comment above phone field: <!-- audit-added -->
       - <!-- NOTE: The phone field will be collected by FormData and sent to Google Apps Script.
            The Apps Script must be updated to map the "phone" key to a new column in the spreadsheet.
            Without that update, phone data will be silently lost. This is OUT OF SCOPE for this plan
            (JS/backend changes are forbidden), but the site owner MUST update the Apps Script separately. -->

    Avoid: Modifying form action, method, or novalidate attribute. Do NOT touch js/main.js. Keep all existing field names unchanged (name, company, email, country, volume, message).
  </action>
  <verify>Open index.html — form has phone field, expanded country list (~20+ options), 3 trust signals visible above form. Existing fields unchanged.</verify>
  <done>AC-1 and AC-2 satisfied: Enhanced fields and trust signals present</done>
</task>

<task type="auto">
  <name>Task 2: Add trust signal CSS and final site-wide polish</name>
  <files>css/styles.css</files>
  <action>
    1. Add trust signal styles (insert before /* ===== TESTIMONIALS SECTION ===== */):
       - .trust-signals: display flex, gap 24px, margin-bottom 28px, flex-wrap wrap, list-style none, padding 0, margin-top 0 <!-- audit-added: reset list styles since trust signals are now a <ul> -->
       - .trust-signal-item: display flex, align-items center, gap 8px, font-size 0.85rem, color rgba(255,255,255,0.65)
       - .trust-signal-item i: color var(--accent-green), font-size 18px

    2. Final responsive polish — check and fix:
       - @media 768px: .trust-signals should stack (flex-direction column, gap 12px)
       - Ensure phone + country form-row collapses to single column on mobile
       - Verify all sections have consistent vertical padding

    3. Polish pass:
       - Ensure no orphaned hover effects from the old orange palette
       - Verify the form focus state box-shadow uses new palette colors (should already be done from Phase 1)
       - Ensure phone input has the same focus-visible outline/box-shadow as other form inputs (inherit existing .form-group input:focus styles) <!-- audit-added: new field must match existing focus states for consistency and accessibility -->
       - Add input[type="tel"]:invalid (when not empty) subtle border indication for pattern mismatch, using CSS :not(:placeholder-shown):invalid selector <!-- audit-added: visual feedback for invalid phone format without blocking submission -->

    Use ONLY CSS custom properties — no hardcoded colors.
    Only ADD new rules. Do not modify existing rules.
  </action>
  <verify>Trust signals styled. Site responsive at all breakpoints. No visual inconsistencies. No old palette remnants.</verify>
  <done>AC-4 satisfied: Final responsive polish complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enhanced inquiry form with phone field, 20+ countries, trust signals, and final site-wide polish</what-built>
  <how-to-verify>
    1. Open index.html in browser
    2. Scroll to Export Inquiry section
    3. Verify: Phone field present (optional, type=tel)
    4. Verify: Country dropdown has 20+ countries
    5. Verify: 3 trust signals visible above form ("Response within 24 hours", etc.)
    6. Verify: Form still submits (check js/main.js is untouched)
    7. Verify: Phone placeholder shows India-based format, not US format <!-- audit-added -->
    8. Verify: Trust signals render as a <ul> list with aria-hidden icons <!-- audit-added -->
    9. Verify: Country select autocomplete is "country-name" <!-- audit-added -->
    10. Test responsive: 375px, 768px, 1024px — form and trust signals reflow
    11. Full scroll test: verify ALL sections look consistent at each breakpoint
    12. ACTION REQUIRED: Update Google Apps Script to handle "phone" field before going live <!-- audit-added -->
    13. Confirm: This is ready to ship as v0.1
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- js/main.js (absolutely no JavaScript changes — webhook must stay intact)
- Form action, method, novalidate attributes
- Existing field name attributes (name, company, email, country, volume, message)
- Any section HTML from Phase 1 or Phase 2 (hero, products, videos, heritage, process, testimonials, FAQ)
- assets/ images (no changes to existing images)

## SCOPE LIMITS
- This plan enhances the form and polishes — no new sections
- No new pages
- No animation changes
- No font or color palette changes (Phase 1 identity is final)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Phone field added with type="tel" and autocomplete="tel"
- [ ] Phone field has pattern attribute for basic format validation <!-- audit-added -->
- [ ] Phone placeholder uses India-based format (not US-centric) <!-- audit-added -->
- [ ] Country select uses autocomplete="country-name" (not "country") <!-- audit-added -->
- [ ] Country dropdown has 20+ options
- [ ] 3 trust signals visible with icons, rendered as semantic <ul> <!-- audit-added -->
- [ ] Trust signal icons have aria-hidden="true" <!-- audit-added -->
- [ ] Trust signals responsive (stack on mobile)
- [ ] js/main.js completely untouched
- [ ] Form action/method unchanged
- [ ] No hardcoded colors in new CSS
- [ ] All sections responsive at 375px, 768px, 1024px, 1440px
- [ ] No visual remnants of old palette anywhere
- [ ] Phone field focus styles match existing form fields <!-- audit-added -->
- [ ] HTML comment documents webhook update requirement for phone field <!-- audit-added -->
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 2 auto tasks completed
- Human verification checkpoint approved
- Form webhook verified intact
- Site ready to ship as v0.1
</success_criteria>

<output>
After completion, create `.paul/phases/03-contact-form-polish/03-01-SUMMARY.md`
</output>
